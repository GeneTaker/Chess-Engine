#include <iostream>


int main() {
    std::cout << "Chess Engine starting..." << std::endl;

    // bool is_white = true;
    // Board board;

    // int t = 0;

    // while (!(board.is_over(is_white))) {
    //     std::vector<Move> moves = board.find_legal_moves(is_white);
    //     if (moves.empty()) return 1;
    //     board.move(moves[0], is_white);

    //     std::cout << "turn " << t << std::endl;
    //     std::cout << "to " << moves[0].to << " and also moved " << moves[0].type << endl;
        
        
    //     is_white = !is_white;
    //     t++;

    // }

    // std::cout << is
    // return 0;
}